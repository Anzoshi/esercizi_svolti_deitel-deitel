/*input numero di filiali, per ogni figliale il numero di clienti, per ogni cliente di ogni filiale età e numero di cd.
output: per ogni fliliale il numero di cd acquistati dai maggiorenni, "" il numero di cd acquistato dai minorenni
tot venduti
*/
#include <stdio.h>
#define eta 10
#define cd 10
typedef  struct  c_Filiali{ 

int clienti[eta][cd] } 1_filiale;

int main(int argc, const char * argv[]) {

1.filiale filialeA;
filialeA.clienti[50][50];
int i;
int j;
                                              for(i=0; i<eta; i++) {
                                                printf("\n")
                                                	for(j=0; j<cd; j++) {
                                                	scanf("%d",filialeA.clienti)
                                                	printf("%d",)
                                                              
                                                              return 0;}
                                                              
